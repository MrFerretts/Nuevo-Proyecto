python-telegram-bot==21.6
python-dotenv==1.0.1
aiosqlite==0.20.0
aiohttp==3.10.10
apscheduler==3.10.4
matplotlib==3.9.2
# soccerdata is optional - FBref stats work when installed but bot runs fine without it
# Install manually: pip install soccerdata>=1.8.0 (requires lxml/libxml2)
